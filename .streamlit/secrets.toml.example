# Copy this file to .streamlit/secrets.toml (gitignored, never commit your real key)
# to enable the DeepSeek-written "AI Summary" section on the AI Insights page.
# Without this, the dashboard works exactly the same -- that section just doesn't render.

DEEPSEEK_API_KEY = "sk-..."
